{% extends 'base.html.twig' %}

{% block title %}Reponse{% endblock %}

{% block body %}
    <h1>Reponse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reponse.id }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ reponse.texte }}</td>
            </tr>
            <tr>
                <th>Correcte</th>
                <td>{{ reponse.correcte ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Justification</th>
                <td>{{ reponse.justification }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reponse_index') }}">back to list</a>

    <a href="{{ path('app_reponse_edit', {'id': reponse.id}) }}">edit</a>

    {{ include('reponse/_delete_form.html.twig') }}
{% endblock %}
