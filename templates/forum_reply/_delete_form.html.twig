<form method="post" action="{{ path('app_forum_reply_delete', {'id': forum_reply.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ forum_reply.id) }}">
    <button class="btn">Delete</button>
</form>
