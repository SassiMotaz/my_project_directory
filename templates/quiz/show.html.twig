{% extends 'base.html.twig' %}

{% block title %}Détails du Quiz{% endblock %}

{% block body %}
<div class="container mt-5">

    <h1 class="mb-4 text-primary">📘 Détails du Quiz</h1>

    <table class="table table-striped table-bordered">
        <tbody>
            {% if is_granted('ROLE_PROFESSEUR') %}
                <tr>
                    <th scope="row">ID</th>
                    <td>{{ quiz.id }}</td>
                </tr>
            {% endif %}

            <tr>
                <th scope="row">Titre</th>
                <td>{{ quiz.titre }}</td>
            </tr>

            <tr>
                <th scope="row">Description</th>
                <td>{{ quiz.description }}</td>
            </tr>
        </tbody>
    </table>

    <div class="mt-4">
        <a href="{{ path('app_quiz_index') }}" class="btn btn-secondary me-2">
            <i class="bi bi-arrow-left"></i> Retour à la liste
        </a>

        {% if is_granted('ROLE_PROFESSEUR') %}
            <a href="{{ path('app_quiz_edit', {'id': quiz.id}) }}" class="btn btn-primary me-2">
                ✏️ Modifier
            </a>

            <a href="{{ path('app_question_new', {'quiz': quiz.id}) }}" class="btn btn-success me-2">
                ➕ Ajouter une question
            </a>

            {{ include('quiz/_delete_form.html.twig') }}
        {% endif %}
    </div>

</div>
{% endblock %}
