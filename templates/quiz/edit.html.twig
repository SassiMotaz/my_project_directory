{% extends 'base.html.twig' %}

{% block title %}Modifier le Quiz{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4">Modifier le Quiz</h1>

    <div class="mb-3">
        {{ include('quiz/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <div class="mt-3">
        <a href="{{ path('app_quiz_index') }}" class="btn btn-secondary me-2">Retour à la liste</a>
        {{ include('quiz/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
